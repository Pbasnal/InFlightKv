version: '3.8'

services:
  inflight-kv-1:
    build:
      context: ..
      dockerfile: modules/spring-web/Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SERVER_PORT=8080
    networks:
      - inflight-kv-network
    restart: unless-stopped

  inflight-kv-2:
    build:
      context: ..
      dockerfile: modules/spring-web/Dockerfile
    ports:
      - "8081:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SERVER_PORT=8080
    networks:
      - inflight-kv-network
    restart: unless-stopped

  inflight-kv-3:
    build:
      context: ..
      dockerfile: modules/spring-web/Dockerfile
    ports:
      - "8082:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=docker
      - SERVER_PORT=8080
    networks:
      - inflight-kv-network
    restart: unless-stopped

networks:
  inflight-kv-network:
    driver: bridge
